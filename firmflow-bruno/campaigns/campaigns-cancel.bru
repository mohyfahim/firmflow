meta {
  name: campaigns-cancel
  type: http
  seq: 6
}

post {
  url: {{url}}/api/v1/projects/{{project_id}}/campaigns/{{campaign_id}}/cancel
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

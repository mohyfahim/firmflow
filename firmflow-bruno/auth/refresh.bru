meta {
  name: refresh
  type: http
  seq: 4
}

post {
  url: {{url}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

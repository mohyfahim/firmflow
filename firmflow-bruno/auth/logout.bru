meta {
  name: logout
  type: http
  seq: 5
}

post {
  url: {{url}}/api/v1/auth/logout
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

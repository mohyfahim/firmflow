meta {
  name: firmware-delete
  type: http
  seq: 5
}

delete {
  url: {{url}}/api/v1/projects/{{project_id}}/firmwares/{{firmware_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

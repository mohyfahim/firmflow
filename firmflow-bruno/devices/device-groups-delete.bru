meta {
  name: device-groups-delete
  type: http
  seq: 17
}

delete {
  url: {{url}}/api/v1/projects/{{project_id}}/device-groups/{{group_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

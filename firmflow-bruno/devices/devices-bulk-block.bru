meta {
  name: devices-bulk-block
  type: http
  seq: 10
}

post {
  url: {{url}}/api/v1/projects/{{project_id}}/devices/bulk
  body: json
  auth: inherit
}

body:json {
  {
    "action": "block",
    "apply_to_filter": false,
    "filter": {},
    "device_ids": [
      "{{device_id}}"
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

meta {
  name: device-types-delete
  type: http
  seq: 15
}

delete {
  url: {{url}}/api/v1/projects/{{project_id}}/device-types/{{device_type_id}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

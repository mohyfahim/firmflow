meta {
  name: device-groups-patch
  type: http
  seq: 16
}

patch {
  url: {{url}}/api/v1/projects/{{project_id}}/device-groups/{{group_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Pilot fleet (renamed)",
    "description": "Updated description"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

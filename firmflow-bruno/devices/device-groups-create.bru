meta {
  name: device-groups-create
  type: http
  seq: 4
}

post {
  url: {{url}}/api/v1/projects/{{project_id}}/device-groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Pilot fleet",
    "description": "Devices in the pilot program"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

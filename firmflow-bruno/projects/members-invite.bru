meta {
  name: members-invite
  type: http
  seq: 11
}

post {
  url: {{url}}/api/v1/projects/{{project_id}}/members
  body: json
  auth: inherit
}

body:json {
  {
    "email": "member@example.com",
    "role_id": "{{role_id}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
